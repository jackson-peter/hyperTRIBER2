% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{get_edits_vs_reference}
\alias{get_edits_vs_reference}
\title{Identify potential editing sites vs reference genome}
\usage{
get_edits_vs_reference(
  data_list,
  ref_gr,
  stranded = FALSE,
  edits_of_interest,
  sample_type = "treat",
  design_vector,
  min_ref = 3,
  min_targ = 3,
  max_other = 2
)
}
\arguments{
\item{data_list}{List of count data frames (one per sample)}

\item{ref_gr}{GRanges with a \code{ref} column for reference bases}

\item{stranded}{Logical, whether data is strand-aware}

\item{edits_of_interest}{Matrix of ref->target base pairs}

\item{sample_type}{Which group to scan ("treat" or "control")}

\item{design_vector}{Named character vector of sample assignments}

\item{min_ref}{Minimum samples with ref base > 0}

\item{min_targ}{Minimum samples with target base > 0}

\item{max_other}{Maximum samples with non-ref/non-target bases > 0}
}
\value{
Named list (per edit type) of site IDs passing filters
}
\description{
For each edit type (e.g. A->G), finds sites where the reference base
matches, enough samples show the ref and target bases, and few samples
show other (non-edit) bases.
}
